# Smarter Application Infrastructure

Creates the AWS infrastructure required by one Smarter application environment (alpha, beta, next, prod, etcetera).

Prerequisites: The Stack (VPC, EKS, traefik, cert-manager) needs to be fully built beforehand.
